-r requirements.txt

# Tooling only — not needed to serve the API.
ruff==0.16.4

# The eval harness needs `anthropic`, which now ships in requirements.txt
# because the API itself generates answers. `--retrieval-only` needs no key.
